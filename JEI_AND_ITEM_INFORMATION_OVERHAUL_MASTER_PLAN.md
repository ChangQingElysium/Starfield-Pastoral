# StardewCraft JEI 与物品信息系统全面重整规划

> 状态：Phase 0–Phase 4 代码侧已实现；鱼塘来源和商店结构化条件已通过 `classes`、207 项自动化测试与完整 `build`；Phase 5 的客户端交互与视觉验收由用户执行
> 文档位置：仓库根目录，避免当前 `docs/*` 忽略规则漏掉本规划
> 范围：合并 issue 10 与 issue 12，覆盖 JEI 数据正确性、R/U 查询、专服同步、信息架构、视觉排版、本地化和验证
> 取代：`docs/root-archive/JEI_OVERHAUL_PLAN.md`。旧文档仅保留为历史资料，不再作为实施依据

## 1. 结论与设计决策

当前 JEI 不能继续通过增加硬编码页面和补几个翻译键修复。它同时存在数据适配、注册生命周期、查询语义和视觉排版问题，需要按一个完整功能面重整。

本次规划采用以下决定：

1. JEI 首先是可靠的配方导航器。玩家对物品按 `U` 应看到真实用途，按 `R` 应看到真实来源。
2. 加工机器以运行时真实数据为唯一来源，不再维护第二套手写 JEI 配方真相。
3. 13 个加工机器类别固定注册。服务器同步只更新配方内容，不决定类别是否存在。
4. 所有参与配方查询的输入、燃料和输出都必须注册为 JEI slot，不能只画成文字或图标。
5. Lookup Anything 的价值在于统一的信息模型，不是把所有信息塞进同一张大卡片。商店、钓鱼和获取来源使用统一索引，再分别展示。
6. 所有界面使用仓库已有的真实 Stardew GUI 资源。禁止继续用纯色矩形模拟木框和羊皮纸。
7. 文本排布以中文、德语、俄语等真实长文本为设计输入。不能只在英文短字符串下看起来勉强可用。
8. 无法精确推导的随机或动态结果要诚实表示，不制造虚假的单一预览。

参考实现：

- [JEI 官方 API](https://github.com/mezz/JustEnoughItems/tree/1.21.1/CommonApi/src/main/java/mezz/jei/api)
- [Farmer's Delight JEI](https://github.com/vectorwing/FarmersDelight/tree/1.21/src/main/java/vectorwing/farmersdelight/integration/jei)
- [Create JEI](https://github.com/Creators-of-Create/Create/tree/mc1.21.1/dev/src/main/java/com/simibubi/create/compat/jei)
- [Lookup Anything](https://github.com/Pathoschild/StardewMods/tree/develop/LookupAnything)

## 2. 用户目标与验收场景

玩家打开 JEI 时通常正在完成一件具体事情：

- 手里有葡萄，想知道能加工成什么。
- 看见葡萄酒，想知道需要什么机器和材料。
- 第一次拿到鱼，不知道熏鱼机是否还需要燃料。
- 想知道一种鱼在哪里、什么季节、什么天气能够钓到。
- 想知道物品在哪个商店出售，价格和条件是什么。
- 想知道一份配方为何还没解锁。

完成后的最低体验标准：

- 葡萄按 `U`：能看到酒桶葡萄酒、腌菜罐果酱、脱水机葡萄干或水果干等真实用途。
- 葡萄酒按 `R`：能看到葡萄、酒桶、加工时间和品质规则。
- 鱼按 `U`：熏鱼机页面同时显示鱼和煤，且两者都参与 JEI 查询。
- 蘑菇按 `U`：显示脱水机，不错误显示腌菜罐。
- 水晶复制机和种子制造机不再因为没有固定 `outputId` 而空白；孵化器按真实运行逻辑显示为消耗型流程，不伪造物品产物。
- 制作页面不出现 `null`、`default`、`s Farming 1` 等内部数据文本。
- 同一种鱼的多个地点、时间段和天气条件不会被第一条规则覆盖。
- 专用服务器连接和数据同步后，类别仍然完整，配方能刷新。
- 小窗口、高 GUI scale 和长翻译下不重叠、不越界、不被毫无提示地截断。

## 3. 明确的非目标

第一轮重整不做以下事情：

- 不为了 JEI 强行迁移全部加工数据到 Minecraft `RecipeManager`。
- 不新造一套与星露原版无关的视觉语言。
- 不用“任意鱼”“任意水果”等虚构占位物品代替真实 JEI Ingredient。
- 不把怪物图鉴、博物馆百科、NPC 喜好等尚未进入本 issue 的内容一起扩张进来。
- 不在每个类别内部实现独立滚动区。JEI 已负责配方翻页，页面内部滚动会破坏操作习惯。
- 不以 NPC 肖像、装饰花纹等内容挤占核心配方信息。装饰只有在帮助识别时才保留。

## 4. 当前基线与必须解决的根因

### 4.1 机器 ID 重复拼接

`ArtisanRecipeDataManager` 使用完整 ID，例如 `stardewcraft:keg`。现有 JEI 再拼一次 `stardewcraft:`，最终得到无效的 `stardewcraft:stardewcraft:keg`。

影响：

- 加工类别可能全部无法注册。
- 配方和 catalyst 无法注册。
- 动态 `RecipeType` 路径可能包含非法冒号。

### 4.2 标签输入、额外输入和动态输出缺失

现有 `DisplayRecipe` 只能容纳一个输入、一个输出和一个机器图标，无法表达：

- `inputTag`
- 熏鱼机所需煤
- 保留输入品质
- 固定输出品质
- `COPY_INPUT`
- `SEEDMAKER`
- `SMOKED`
- 果酱、腌菜等携带来源数据的产物
- 输出数量范围和概率

### 4.3 类别注册依赖晚到的同步数据

JEI 类别注册发生在配方注册之前，运行时只能增删配方，不能补注册一个初始化阶段不存在的类别。

因此类别必须由稳定描述表决定，不能遍历当前同步快照动态决定。

### 4.4 内部条件语法直接泄漏给玩家

制作类别直接绘制 `unlockCondition` 原始字符串，导致 `null`、`default`、技能条件代码和节日 ID 进入界面。

### 4.5 信息数据被压扁

- 商店条目丢失以物换物、日期、矿井层数、邮件和通用条件。
- 钓鱼按物品 ID 去重，只保留第一条规则。
- 晶球产物同时存在硬编码表和同步表，可能重复或漂移。
- 水果蔬菜分类同时依赖标签和手写集合，已经出现命名错误。

### 4.6 视觉组件与仓库现有系统割裂

当前 `JeiDrawHelper` 使用纯色填充绘制面板和槽位，但仓库已经有：

- `CommonGuiTextures.drawTextureBox`
- `CommonGuiTextures.drawEntryBox`
- `CommonGuiTextures.drawItemSlot`
- Stardew 箭头、金币、滚动横幅和品质图标资源
- `GuiText` 的居中、截断和换行工具

JEI 应复用这些现有组件，不再维护一套低保真绘制逻辑。

## 5. 目标架构

### 5.1 总体数据流

```text
服务端数据包 / 单机资源重载
            ↓
现有权威数据管理器
  Artisan / Cooking / Crafting / Shop / Fishing / Geode
            ↓
只读 JEI 适配层
  规范化输入、输出、条件、概率、来源
            ↓
ItemInformationIndex
  usesByItem / sourcesByItem / shopsByItem / fishingByItem
            ↓
JEI 类别
  配方类别 + 信息类别
```

权威数据管理器继续负责游戏行为。JEI 适配层只能读取和解释，不能拥有另一份业务规则。

### 5.2 固定机器描述表

新增一个很小的 `MachineJeiRegistry`，使用 `ResourceLocation` 维护 13 个固定描述：

- 机器 ID
- JEI RecipeType ID
- 机器物品 ID
- 分类标题 Component
- 布局类型，标准、额外燃料或随机输出

固定机器清单：

1. charcoal_kiln
2. cheese_press
3. crystalarium
4. dehydrator
5. fish_smoker
6. furnace
7. incubator
8. keg
9. loom
10. mayonnaise_machine
11. oil_maker
12. preserves_jar
13. seed_maker

描述表在 JEI `registerCategories` 阶段无条件注册。某台机器当前没有配方时，类别可以为空或隐藏，但类型必须已经存在。

### 5.3 加工配方展示模型

现有单输入单输出 `DisplayRecipe` 替换为能表达真实配方的模型：

```text
ArtisanDisplayRecipe
  recipeId
  machineId
  primaryInputs[]
  auxiliaryInputs[]
  outputs[]
  durationMinutes
  qualityRule
  notes[]
```

每个输入必须包含：

- `Ingredient` 或可轮播的真实 ItemStack 列表
- 数量
- JEI 角色
- 是否消耗

每个输出必须包含：

- 所有真实可查询的 ItemStack
- 固定数量或数量范围
- 概率
- 品质规则
- 是否继承输入来源、颜色或品质

### 5.4 唯一加工适配器

新增 `ArtisanJeiRecipeFactory`，只负责把 `ArtisanRecipeDataManager.Recipe` 转为展示模型。

它必须覆盖：

| 输入模式 | 处理方式 |
|---|---|
| inputId | 单物品 Ingredient |
| inputTag | 直接使用标签 Ingredient，保留所有候选物品 |
| CROP_TYPE | 从权威物品数据分类展开，不使用手写作物集合 |
| MINERAL_TYPE | 从权威物品数据分类展开 |
| FISH_TYPE | 从权威鱼类目录展开 |

| 输出模式 | 处理方式 |
|---|---|
| FIXED | 精确输出物品、数量和品质 |
| COPY_INPUT | 为每个输入生成对应输出，保留 JEI 查询关系 |
| SMOKED | 生成携带原鱼身份和品质的烟熏鱼 ItemStack |
| SEEDMAKER | 展示主种子、混合种子、上古种子等真实分支及概率、数量范围 |

果酱、腌菜、鱼籽、干果和干蘑菇使用现有 subtype 数据生成真实输出栈，不能只显示一个没有来源信息的通用物品。

### 5.5 同步生命周期

JEI 生命周期调整为：

1. `registerCategories`：注册全部固定类别。
2. `registerRecipes`：读取客户端此刻已有的数据快照并发布。
3. `onRuntimeAvailable`：保存 runtime，不创建新类别。
4. 收到 `DataRegistrySyncPayload`：对每个已注册 RecipeType 执行旧配方隐藏/移除和新配方添加。
5. 资源重载或断开服务器：清理已发布快照，避免跨服务器残留。

刷新必须是幂等的。连续收到相同快照不能产生重复页面。

### 5.6 物品信息索引

建立客户端只读 `ItemInformationIndex`，灵感来自 Lookup Anything，但范围保持克制：

```text
usesByItem       物品可参与的制作、烹饪和加工
sourcesByItem    能产出该物品的制作、加工和晶球规则
shopsByItem      出售或交换该物品的商店条目
fishingByItem    该物品的全部钓鱼规则
```

索引从各权威快照构建，不再在每个 JEI Category 中各自遍历和去重。

第一阶段索引只服务 JEI。未来若制作独立 Lookup Anything 风格查询界面，可以复用同一索引，无需再重写数据层。

## 6. 类别范围与信息职责

### 6.1 本轮必须完成

| 类别 | 数据来源 | R/U 语义 | 本轮目标 |
|---|---|---|---|
| 13 个加工机器 | ArtisanRecipeDataManager | 输入为 U，输出为 R | 全面重写 |
| Stardew 制作 | StardewCraftingRecipeData | 材料为 U，产物为 R | 修条件与排版 |
| 烹饪 | VanillaCookingRecipeData | 食材为 U，菜品为 R | 正式新增 |
| 晶球加工 | 晶球权威数据 | 晶球为 U，矿物为 R | 去重并统一来源 |
| 商店来源 | ShopRegistry / 同步目录 | 商品为 R 来源 | 保留完整交易条件 |
| 钓鱼来源 | FishingDataManager | 鱼为 R 来源 | 聚合全部规则 |

### 6.2 后续可扩展，但不与 P0 混做

只有对应游戏行为和权威数据已经稳定后，再增加：

- 木桶陈酿
- 回收机
- 蜂房
- 树液采集器
- 蟹笼
- 怪物掉落

这些类别不得从旧文档复制硬编码概率表。必须先找到或建立游戏实际使用的权威定义。

## 7. 视觉与排版系统

### 7.1 设计方向

色彩策略：受控的 Stardew 完整配色。面板使用真实像素资源，颜色用于语义和层级，不用大块纯色区分普通字段。

使用场景：玩家在小窗口或全屏 Minecraft 中打开 JEI，通常一边对照背包物品，一边快速确认机器、材料和条件，前景必须清楚、紧凑且能在几秒内扫读。

视觉锚点：

- Stardew Valley 原版菜单、商店和物品槽
- Farmer's Delight 的直接配方阅读顺序
- Lookup Anything 的字段分组和动态结果诚实表达

### 7.2 面板资源

优先复用：

- `CommonGuiTextures.drawTextureBox`：页面外框
- `CommonGuiTextures.drawEntryBox`：条件、概率等次级信息区
- `CommonGuiTextures.drawItemSlot`：所有普通物品槽
- 现有金币、箭头、品质、季节、技能图标
- `GuiText.drawCenteredClamped`
- `GuiText.drawWrapped`

`JeiDrawHelper.drawPanel`、`drawSlotBg` 等纯色绘制逐步退役。保留的 JEI helper 只负责 JEI 特有布局，不再复制通用视觉组件。

### 7.3 统一尺寸和间距

JEI 页面统一使用 176 像素宽度，避免当前 160、166 混用造成布局漂移。

基础 token：

| 项目 | 规格 |
|---|---|
| 外边距 | 6 px |
| 内容区左右内边距 | 8 px |
| 普通物品槽 | 18 x 18 px |
| 重点输出槽 | 24 x 24 px，使用真实纹理，不画程序光晕 |
| 槽间距 | 4 px |
| 正文字号 | Minecraft font 原始 1x，不缩放拟合 |
| 正文行高 | `font.lineHeight + 2` |
| 标题与正文间距 | 4 px |
| 信息分组间距 | 5 px |
| 单字段最大行数 | 2 行 |

禁止通过缩小字体解决溢出。优先顺序为：重组字段、换行、移入 tooltip、最后才是带明确 tooltip 的省略。

### 7.4 文字层级

每页只允许三层文字：

1. 主要值：机器结果、商店名或钓鱼地点。
2. 字段值：时间、价格、数量、季节。
3. 次要说明：解锁条件、概率备注、品质继承。

不在狭窄页面中重复 JEI 已经显示的类别标题和物品名称。物品名称交给槽位 tooltip，页面正文只展示玩家无法从物品图标直接得知的信息。

所有文字使用 `Component`。布局计算使用 `font.width(Component)` 或 `font.split(Component)`，不提前 `.getString()` 后自行截字符。

### 7.5 长文本规则

- 商店名最多 2 行，不截断所有者名。
- 解锁条件最多 2 行，完整内容同时放入 hover tooltip。
- 地点列表最多显示 2 行，更多地点显示“另有 N 个地点”，hover 展开完整列表。
- 多个时间段优先合并为紧凑格式，例如 `6:00-9:00 / 18:00-26:00`。
- 季节、天气、品质和星期优先使用原版图标加 tooltip，不重复写长标签。
- 价格、数量和概率始终与对应物品槽保持空间关联，不散落在页面边缘。

### 7.6 交互规则

- 所有真实材料、燃料和结果都是 JEI slot，可被聚焦、点击和查询。
- 机器本体注册为 catalyst，不伪装成普通输入。
- hover 次级信息区显示完整条件、概率说明和数据来源。
- 不增加自定义弹窗。
- 不在类别内部增加滚动条。
- JEI 翻页时页面结构保持稳定，避免字段位置随内容长度大幅跳动。

## 8. 各类别布局规格

### 8.1 标准加工页面，176 x 64

```text
┌──────────────────────────────────────────┐
│  [主输入]  →  [机器]  →  [重点输出]       │
│   x5                         x1           │
│          时间图标 2小时  |  品质规则       │
└──────────────────────────────────────────┘
```

规则：

- 主视觉只有一条从输入到输出的阅读线。
- 数量由 ItemStack count 和槽位 tooltip 共同表达，不在远处重复一遍。
- 处理时间置于底部信息带，不悬浮在机器上方。
- 固定品质、继承品质只在确实影响结果时显示。

### 8.2 多输入加工页面，176 x 72

用于熏鱼机和未来需要燃料的机器。

```text
┌──────────────────────────────────────────┐
│ [鱼] + [煤]  →  [熏鱼机]  →  [烟熏鱼]    │
│              时间图标 50分钟             │
│              保留原鱼品质                 │
└──────────────────────────────────────────┘
```

煤必须作为 `RecipeIngredientRole.INPUT` 注册，不能只画一个煤图标。

### 8.3 随机输出页面，176 x 84

用于种子制造机等多分支规则。

```text
┌──────────────────────────────────────────┐
│ [作物] → [种子制造机] → [对应种子 1-3]    │
│                       [混合种子 1-4]      │
│                       [上古种子 0.5%]     │
│ 时间图标 20分钟  |  结果随机              │
└──────────────────────────────────────────┘
```

每个输出分支都是 OUTPUT slot。概率和数量范围紧邻对应槽位。

### 8.4 Stardew 制作页面，176 x 72

现有数据最多 4 种材料，保留 2 x 2 输入网格，不再假设未来无限增加。

```text
┌──────────────────────────────────────────┐
│ [材料][材料]       →       [重点输出]      │
│ [材料][材料]                              │
│ 解锁：耕种等级 6                          │
└──────────────────────────────────────────┘
```

解锁条件必须先转换成结构化条件，再本地化显示。无条件或默认拥有的配方不显示条件行。

### 8.5 烹饪页面，176 x 82

```text
┌──────────────────────────────────────────┐
│ [食材][食材][食材]  →  [料理]             │
│ [食材]                                   │
│ 能量 75  |  生命 33  |  产出 x1           │
│ 来源：酱料女王 / 好感 / 技能              │
└──────────────────────────────────────────┘
```

标签食材使用真实 Ingredient 轮播。任意鱼、任意蛋和任意奶都必须能被实际物品按 `U` 查到。

### 8.6 商店来源页面，176 x 82

```text
┌──────────────────────────────────────────┐
│ [商品]  商店名称                          │
│         [金币] 120  或  [交换物] x5       │
│         每次获得 x10  |  库存 x1          │
│         春夏  周五  第2年起               │
└──────────────────────────────────────────┘
```

规则：

- 商品是 OUTPUT。
- 交换物是 INPUT，因此交换物按 `U` 也能找到这项用途。
- 金币价格和物品交换二选一或同时显示，以真实数据为准。
- NPC 肖像最多 24 x 24，仅在存在可靠 owner 映射时显示；缺少肖像时布局不留空洞。
- 邮件、矿井层数和复杂 `availableWhen` 进入底部条件行和 tooltip。

### 8.7 钓鱼来源页面，176 x 112

不再把鱼放大到 32 x 32 后只留下极窄的文字列。鱼使用 24 x 24 重点槽，右侧和下方分组展示规则。

```text
┌──────────────────────────────────────────┐
│ [鱼]  难度 45  冲刺型                     │
│ 地点：海洋、姜岛南部                      │
│ 时间：6:00-19:00                         │
│ [春][夏][秋] [晴]  钓鱼等级 3             │
│ 深度 2+  |  另有 3 个地点                 │
└──────────────────────────────────────────┘
```

同一物品的规则先聚合，再按可理解的条件组合分组。完全相同条件合并地点；条件不同则保留独立 JEI 页面，不能全部糊成一段话。

难度不使用五个星号占掉半行。显示数值和移动类型，颜色只作辅助，不作为唯一信息。

### 8.8 晶球加工页面，176 x 60

```text
┌──────────────────────────────────────────┐
│ [晶球]  →  [铁匠砧/处理入口]  →  [产物]  │
│                        概率或来源池        │
└──────────────────────────────────────────┘
```

晶球产物只能来自权威晶球表。移除 JEI 内重复的硬编码矿物池。

### 8.9 鱼塘产物页面，176 x 102

鱼、鱼塘管理器和具体产物分别是真实 `INPUT / CATALYST / OUTPUT` 槽。鱼籽必须保留来源鱼风味，使玩家对某种鱼籽按 `R` 能反查到对应鱼塘，而不是只看到腐乳罐用途。

页面同时显示解锁该产物的最低鱼数、产物自身抽取率、随鱼数增长的每日产出率、鱼籽追加数量以及金色动物饼干翻倍规则。不单独复制鱼塘 JSON，从 `FishPondDataService` 的运行时快照投影展示数据。

## 9. 条件与本地化系统

### 9.1 条件解析

旧制作数据中的字符串先转换为结构化条件：

| 原始值 | 玩家显示 |
|---|---|
| `null` | 不显示条件 |
| `default` | 不显示条件，表示默认已知 |
| `s Farming 1` | 耕种等级 1 |
| `s Combat 7` | 战斗等级 7 |
| 其他 legacy token | 对照原版语义解析，不允许原样回退到界面 |

解析失败时记录带 recipe ID 的日志，并在开发环境显示安全的“未知解锁条件”，不能显示内部代码。

### 9.2 翻译范围

任何新增或改动的玩家可见键都同步更新当前全部语言文件：

- de_de
- en_us
- es_es
- fr_fr
- hu_hu
- it_it
- ja_jp
- ko_kr
- pt_br
- ru_ru
- tr_tr
- zh_cn

优先复用 `源文件/Content` 中的星露官方翻译。没有官方文本时，英文作为明确回退，但不能把翻译 key 或内部 ID 当成显示文本。

### 9.3 文案原则

- 字段标签尽量短，例如“时间”“品质”“来源”。
- 不重复物品 tooltip 已经提供的名称。
- 不使用只有英文环境才能理解的缩写，例如 `Y2+`、`Sp`、`Fa`。
- 时间遵循星露格式，正确支持 24:00 到 26:00。

## 10. 文件组织规划

目标不是大量拆类，而是把职责边界恢复清楚。

```text
integration/jei/
  StardewJeiPlugin.java              只协调 JEI 生命周期
  MachineJeiRegistry.java            13 个稳定机器描述
  ArtisanJeiRecipeFactory.java       加工数据到展示模型
  ItemInformationIndex.java          用途与来源索引
  JeiTheme.java                      JEI 尺寸与视觉 token
  JeiText.java                       JEI 长文本与 tooltip 规则
  category/
    ArtisanRecipeCategory.java
    StardewCraftingCategory.java
    CookingRecipeCategory.java
    ShopSourceCategory.java
    FishingSourceCategory.java
    GeodeProcessingCategory.java
```

如果实现过程中发现 `JeiTheme` 和 `JeiText` 只需要极少方法，可以继续留在现有 helper 中，不为了目录好看而制造空抽象。

`StardewJeiPlugin` 中的 subtype 注册、GUI handler 和 recipe category 注册要分成清晰私有段落，但没有必要拆成十几个 registrar 类。

## 11. 分阶段实施

### Phase 0：建立安全网

实施状态：已完成。固定机器注册表、资源合同和关键动态规则已有自动化测试覆盖。

目标：在改变展示前锁定数据和查询行为。

- 为 13 个机器描述写完整性测试。
- 测试机器 ID 往返后不会重复命名空间。
- 为每个 artisan JSON 建立“运行时能匹配，JEI 也能枚举”的合同测试。
- 记录当前客户端和专服同步后的配方数量，作为回归基线。
- 删除测试中对硬编码 `PreservesCropTypeHelper` 结果的依赖，改查权威标签或物品数据。

验证：`gradle test` 中相关合同测试全部通过。

### Phase 1：修复加工机器 P0

实施状态：已完成。13 个类别、真实输入/输出、额外煤炭、动态与随机产物、同步刷新和三类页面布局均已接入；实际游戏视觉验收留到 Phase 5。

目标：让 issue 12 的酒、果酱、熏鱼、鱼籽和机器查询正确工作。

- 固定注册 13 个机器类别。
- 使用 `ResourceLocation` 贯穿机器标识。
- 引入完整加工展示模型。
- 支持标签输入、动态输入、额外燃料和四种输出模式。
- 修复同步刷新和断线清理。
- 完成标准、多输入、随机输出三种布局。
- 移除已无调用者的旧变体构建方法。

验证案例：葡萄、蓝莓、欧洲萝卜、任意鱼、煤、任意矿物、任意可制种作物。

### Phase 2：制作与烹饪

实施状态：已完成。制作材料候选项、语义化解锁条件、正式烹饪类别、标签/类别食材查询以及能量/生命信息均已接入；默认已知配方不显示无意义的条件行。

目标：所有配方输入输出都能正确使用 R/U 查询。

- 解析制作解锁条件。
- 保留 2 x 2 制作输入布局并重做条件区。
- 新增正式烹饪类别。
- 标签和类别食材注册所有真实候选。
- 展示能量、生命和解锁来源，但不重复无用说明。

验证：任意鱼、任意蛋、任意奶、4 材料配方、默认配方、技能解锁配方。

### Phase 3：商店、钓鱼、晶球与鱼塘来源

实施状态：代码侧已完成。商店同步保留交换物、购买数量、库存和内置结构化条件，未知扩展条件安全回退为通用提示；钓鱼保留同一物品的独立规则；晶球页直接消费运行时权威产物池；鱼塘页为鱼籽及其他产物建立可反查来源。这些查询关系由各类别的真实 JEI slot 建立。

目标：Lookup Anything 风格的来源信息准确且可扫读。

- 建立 `ItemInformationIndex`。
- 商店保留金币、交换物、库存、购买堆叠和全部条件。
- 钓鱼取消按物品 ID 粗暴去重，按条件组合聚合。
- 晶球只使用一个权威来源。
- 鱼籽和其他鱼塘产物可以通过 `R` 查到来源鱼与鱼塘。
- 完成信息类别的长文本和 tooltip 行为。

验证：多商店商品、以物换物、星期限定商品、多地点多时间鱼、同矿物来自多晶球。

### Phase 4：视觉整合与本地化

实施状态：代码侧已完成。本轮涉及的加工、制作、烹饪、商店、钓鱼、晶球和鱼塘页统一为 176 px，并使用现有 Stardew GUI 资源；所有 JEI 键已在全部 12 个语言文件中对齐。不同语言、GUI scale 和小窗口的实机截图检查仍属于 Phase 5，不在此处冒充完成。

目标：去掉当前纯色程序面板和挤压排版。

- JEI 改用 `CommonGuiTextures` 和 `GuiText`。
- 统一 176 px 页面宽度和布局 token。
- 替换程序绘制槽位、箭头和光晕。
- 更新全部语言文件。
- 对中文、英文、德语、俄语、日语至少进行一次视觉检查。

验证：不同语言下无重叠、越界、裸翻译键和无提示截断。

### Phase 5：实际游戏验收

目标：确认静态正确性与玩家实际操作一致。

- 单机新世界。
- 已有存档。
- 专用服务器首次连接。
- 服务器数据包重载后刷新。
- JEI 安装与未安装两种环境。
- 小窗口与全屏。
- GUI scale 2、3、4，以及 Auto。
- 从物品列表、背包、工作台和 V 菜单触发 R/U。

## 12. 自动化测试矩阵

### 12.1 数据合同测试

- 每个机器 ID 都能解析为有效 `ResourceLocation`。
- 每个机器描述都能找到真实机器物品。
- 每条 artisan 配方至少能生成一个有效 JEI 展示结果，明确标记无法静态展开的规则除外。
- 所有 `inputTag` 至少包含一个物品。
- `COPY_INPUT`、`SMOKED`、`SEEDMAKER` 都有专门覆盖。
- 熏鱼机展示模型包含煤。
- subtype 结果保留来源和品质。

### 12.2 查询关系测试

- 每个输入物品都出现在 INPUT slot。
- 每个结果物品都出现在 OUTPUT slot。
- 每个机器都作为 CATALYST 注册。
- 商店交换物作为 INPUT，商品作为 OUTPUT。
- 标签候选物逐一能查到对应用途。

### 12.3 同步测试

- 类别在数据同步前已经存在。
- 第一次同步后加入配方。
- 第二次相同同步不重复。
- 不同服务器快照能替换旧配方。
- 断开连接不会保留上个服务器的数据。

### 12.4 本地化测试

- 所有 `stardewcraft.jei.*` 键在全部语言中存在。
- 玩家界面中不出现 `null`、`default` 和 legacy 条件原码。
- 商店没有翻译时使用可读的 definition 名称或安全回退，不显示裸 key。

### 12.5 视觉测试清单

每个类别至少截图以下组合：

- 最短英文文本。
- 中文。
- 最长德语或俄语文本。
- 最大字段数量。
- 无可选条件。
- 多个可选条件。
- GUI scale 2 和 4。

人工检查：

- 阅读顺序是否从输入自然走向输出。
- 数量、概率和价格是否紧邻对应槽位。
- 是否存在同一层级五种颜色互相竞争。
- 是否有文字压住物品、边框、箭头或其他文字。
- 省略文字是否始终能通过 hover 查看完整内容。

## 13. 日志与失败策略

客户端日志必须能定位数据问题，但不能刷屏：

- JEI 初始化时记录每个类别的配方数量。
- 同步刷新时只记录旧数量、新数量和快照版本。
- 无效机器 ID、空标签、无法构建输出时带 recipe ID 记录一次警告。
- 正常的空类别不重复警告。
- 玩家界面不显示 Java 异常、内部 ID 或解析代码。

## 14. 完成定义

只有同时满足以下条件，问题 10 与 12 才能关闭：

- 13 个加工机器类别稳定注册并能在专服使用。
- 酒桶、腌菜罐、脱水机、熏鱼机、水晶复制机和种子制造机的特殊规则完整显示；孵化器按真实运行逻辑显示消耗输入且不伪造物品产物。
- 烹饪正式进入 JEI。
- 制作解锁条件全部语义化和本地化。
- 商店、钓鱼、晶球信息不再丢失关键来源条件。
- 鱼籽和其他鱼塘产物可以反查到权威鱼塘规则。
- 所有参与配方的真实物品都能通过 R/U 找到关系。
- 页面使用真实 Stardew GUI 资源，文字层级和间距统一。
- 长翻译、小窗口和高 GUI scale 下没有重叠和越界。
- 自动化测试、`gradle classes` 和实际游戏验收通过。

## 15. 实施边界确认

实施已按 Phase 0 到 Phase 4 的代码顺序推进。当前剩余边界是 Phase 5 的真实交互验收：必须在单机、专服、多语言、不同 GUI scale 下实际打开 JEI 页面并检查 R/U。本轮 `./gradlew classes`、`./gradlew test` 与 `./gradlew build` 均已通过；根据分工，不由代理启动客户端。

确认本规划后，实施中如果发现运行时机器逻辑与 JSON 定义不一致，应先修复权威运行链路，再让 JEI 跟随。不得为了让 JEI 看起来正确而单独伪造展示结果。
