package com.stardew.craft.api.v1.festival;

/** Observable changes to a persisted festival session. */
public enum StardewFestivalSessionEventType {
    PHASE_CHANGED,
    MAP_PHASE_CHANGED,
    PARTICIPANT_JOINED,
    PARTICIPANT_LEFT
}
